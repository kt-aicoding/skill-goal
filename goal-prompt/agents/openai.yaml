interface:
  display_name: "Goal Prompt"
  short_description: "Create durable, verifiable long-run goals"
  default_prompt: "Use $goal-prompt to turn this work request into a durable, verifiable long-running goal prompt."
